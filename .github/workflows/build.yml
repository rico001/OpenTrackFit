name: Build Firmware

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO
        run: pip install platformio

      - name: Generate build info
        run: echo "#define BUILD_TIME \"$(date -u '+%Y-%m-%d %H:%M:%S UTC')\"" > include/build_info.h

      - name: Set firmware filename
        run: |
          if [ "${{ github.event_name }}" = "release" ] || [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "FW_NAME=firmware-main.bin" >> $GITHUB_ENV
          else
            echo "FW_NAME=firmware-dev.bin" >> $GITHUB_ENV
          fi

      - name: Build firmware
        run: pio run

      - name: Rename firmware
        run: cp .pio/build/esp32/firmware.bin "${{ env.FW_NAME }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FW_NAME }}
          path: ${{ env.FW_NAME }}

      - name: Attach to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.FW_NAME }}

